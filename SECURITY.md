# Security Policy

Ballast 处于早期开发阶段。默认配置面向研究回放与纸面执行，**不应**连接持有真实资金的交易账户。

## 支持范围

| 组件 | 当前状态 |
| --- | --- |
| 公共行情 / 纸面执行 | 可本地运行，欢迎报告缺陷 |
| 股票研究（Alpaca IEX） | 仅 market-data，无下单能力 |
| Live 下单 / 私有账户 / 对冲 | 代码路径存在但默认锁定，生产使用前需独立安全审查 |

## 密钥与敏感数据

- 本地通过未提交的 `.env` 或系统密钥存储注入；仓库只允许 `.env.example` 占位符。
- 生产环境使用专用 secret manager；数据库只保存账户元数据与 secret 引用名，不保存密钥明文。
- 日志、错误、指标和事件 payload 不得包含密钥、完整签名材料或可恢复的凭证。
- Alpaca Key 仅用于免费 IEX 历史行情；交易所下单 API Key 当前不得接入。
- `BALLAST_LIVE_ENABLED` 默认 `false`；实现检测到 `true` 会拒绝启动。

## 报告漏洞

请**不要**在公开 issue、讨论区或 PR 中披露可利用的安全问题或真实凭证。

优先使用 GitHub 仓库的 **Security Advisories / private vulnerability reporting**（若已启用）。
若不可用，请邮件联系维护者：`sevenold@outlook.com`（勿在邮件中粘贴真实交易密钥）。

报告请尽量包含：

1. 影响组件与版本 / commit
2. 复现步骤或概念验证（不含真实密钥）
3. 影响范围（信息泄露、未授权操作、拒绝服务等）
4. 你是否已公开讨论过该问题

我们会确认收悉，并在修复可用后协调披露时机。

## 安全基线（贡献者自检）

- [ ] 未提交 `.env`、密钥、证书、客户数据
- [ ] 未在文档中写入真实账户 ID / 内部主机名
- [ ] 未绕过 live 锁、审批或零默认限额
- [ ] 超时/不确定订单路径有对账说明或测试
