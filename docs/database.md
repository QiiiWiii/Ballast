# 数据库边界

PostgreSQL 保存可恢复的业务事实：

- 执行任务及其版本；
- 子订单意图和交易所订单标识；
- 去重后的成交；
- 任务事件；
- 待发送命令 outbox。

数据库不用于保存全部实时盘口和逐笔成交。执行决策需要的行情摘要应随事件保存；完整历史行情在出现明确回放需求后进入专用时序或对象存储。

Node 网关不维护权威业务数据库。网关重启后由 Rust 使用订单、成交和持仓查询进行对账。
