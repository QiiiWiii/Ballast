# Security Policy

Ballast 处于早期开发阶段，目前不应连接持有真实资金的账户。

## 密钥处理

- 本地通过未提交的 `.env` 或系统密钥存储注入。
- 生产环境应使用专用 secret manager。
- 日志、错误、指标和事件 payload 不得包含密钥或完整签名材料。
- API Key 从只读权限开始；开启下单权限必须经过独立审批。

## 报告问题

请不要在公开 issue 中披露可利用的安全问题或真实凭证。通过仓库所有者提供的私有渠道报告。
